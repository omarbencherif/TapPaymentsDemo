# START BACKEND SERVER

**In a NEW terminal window**, run:

```bash
cd /Users/omar/WebstormProjects/TapPayments
npm run server
```

The backend should start and show:
```
🚀 Tap Payments Backend Server Running!
📍 Server URL: http://localhost:4000
```

Then you can test MIT payments in the browser.

